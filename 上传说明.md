# 上传到 GitHub Pages

目标仓库：`https://github.com/yycute0121/yyyycute_project2`

发布后地址：

```
https://yycute0121.github.io/yyyycute_project2/
```

## 方式一：网页直接拖拽（最简单，不用装 Git）

1. 打开 https://github.com/yycute0121/yyyycute_project2
2. 点 **Add file → Upload files**
3. 把解压后 `workbuddy` 文件夹里的**内容**全部拖进去

   注意是把里面的东西拖进去，不要拖整个文件夹，否则会多一层目录，网址要变成 `.../yyyycute_project2/workbuddy/`

4. 底下填个提交说明，点 **Commit changes**
5. 进 **Settings → Pages**
   - Source 选 **Deploy from a branch**
   - Branch 选 **main**，目录选 **/ (root)**
   - 点 Save
6. 等一两分钟，刷新 Pages 页面就能看到网址

### 拖拽时要留意

`.nojekyll` 是隐藏文件，macOS 访达默认看不见。按 `Command + Shift + .` 可以显示隐藏文件，记得把它一起拖上去。

这个文件的作用：告诉 GitHub Pages 不要用 Jekyll 处理，否则下划线开头的文件可能不被发布。

## 方式二：命令行（Git 已装好的话）

```bash
cd workbuddy
git init
git add .
git commit -m "feat: WorkBuddy 四模块与本地备份"
git branch -M main
git remote add origin https://github.com/yycute0121/yyyycute_project2.git
git push -u origin main
```

推送时会要求认证。GitHub 已经不支持账号密码，需要用 **Personal Access Token**：

1. GitHub 右上头像 → Settings → Developer settings
2. Personal access tokens → Tokens (classic) → Generate new token
3. 勾选 `repo` 权限，生成后复制
4. push 时用户名填 `yycute0121`，密码位置粘贴这个 token

推送完成后同样去 **Settings → Pages** 按上面第 5 步设置。

## 手机上离线使用

1. 手机联网打开 `https://yycute0121.github.io/yyyycute_project2/`
2. iOS Safari：分享按钮 → **添加到主屏幕**
   Android Chrome：右上菜单 → **添加到主屏幕**
3. 之后从桌面图标打开，断网也能用

## 重要提醒

数据只存在你打开它的那台设备的浏览器里，不会同步到云端，也不会在手机和电脑之间共享。

所以：

- 手机和电脑各自的数据是独立的
- 清理浏览器数据、卸载 PWA 都会**删除全部记录**
- 换设备之前，先在「数据与存储」页点 **导出备份**，拿到 ZIP 文件收好
- 新设备上打开后，用 **导入恢复** 选那个 ZIP 就能全部还原

应用会在超过 14 天没备份时在首页提醒你，这个周期可以在设置里改成 7 / 30 天或关掉。

## 以后要改代码的话

改完 `css/` 或 `js/` 里的文件后，**务必把 `service-worker.js` 第 14 行的版本号加一**：

```js
var CACHE = 'workbuddy-v7';   // 改成 v8
```

不改的话浏览器会继续用缓存里的旧文件，你会看到改动没生效。

## 本地预览

直接双击 `index.html` 也能用，只是离线缓存不会启用。想完整测试就起个静态服务：

```bash
cd workbuddy
python3 -m http.server 8777
```

然后浏览器打开 `http://127.0.0.1:8777`。
