# WorkBuddy GitHub 部署指南

> 目标仓库：`https://github.com/yycute0121/yynotes`  
> 发布后地址：`https://yycute0121.github.io/yynotes/`

---

## 一、首次部署（已完成）

代码已推送到仓库，只需开启 GitHub Pages：

1. 打开 https://github.com/yycute0121/yynotes/settings/pages
2. **Source** 选 **Deploy from a branch**
3. **Branch** 选 `main`，目录选 `/ (root)`
4. 点 **Save**
5. 等 1-2 分钟，刷新页面即可看到发布地址

---

## 二、后续更新代码

### 方式一：让 WorkBuddy 帮你推送（推荐）

1. 修改 `/workspace/workbuddy/` 里的文件
2. 生成一个新的 **Personal Access Token**：
   - 打开 https://github.com/settings/tokens
   - 点 **Generate new token (classic)**
   - 勾选 **`repo`** 权限
   - 生成后**立即复制**（只显示一次）
3. 把 Token 发给 WorkBuddy，说"帮我推送到 GitHub"
4. WorkBuddy 会自动完成推送
5. **推送完成后删除这个 Token**（安全习惯）

### 方式二：直接在 GitHub 网页修改

适合改小内容（如文案、数字）：

1. 打开 https://github.com/yycute0121/yynotes
2. 找到要改的文件，点右上角 **铅笔图标**（Edit）
3. 修改后点 **Commit changes...**
4. 填提交说明，点 **Commit changes**

> ⚠️ 改完 `css/` 或 `js/` 文件后，**务必提升 `service-worker.js` 里的版本号**，否则浏览器会用缓存：
> ```js
> var CACHE = 'workbuddy-v7';  // 改成 v8、v9...
> ```

### 方式三：命令行推送（会 Git 的话）

```bash
cd /workspace/workbuddy
git add .
git commit -m "描述这次改了什么"
# 推送需要 Token，见方式一的步骤 2
git push origin main
```

---

## 三、重要提醒

| 事项 | 说明 |
|------|------|
| **数据不共享** | 手机和电脑各自独立，数据只存在打开它的那台设备 |
| **清理浏览器 = 删数据** | 清理缓存或卸载 PWA 会丢失全部记录 |
| **换设备前必备份** | 在「数据与存储」页点 **导出备份**，拿到 ZIP 收好 |
| **新设备恢复** | 打开应用 → **导入恢复** → 选之前的 ZIP |
| **备份提醒** | 超过 14 天没备份时首页会提醒，可在设置里改周期 |

---

## 四、常见问题

**Q：推送时提示认证失败？**  
A：GitHub 已不支持密码登录，必须用 Personal Access Token。见「方式一」步骤 2。

**Q：改了代码但网页没变化？**  
A：检查 `service-worker.js` 里的 `CACHE` 版本号是否已提升，然后强制刷新浏览器（Ctrl+F5 或 Command+Shift+R）。

**Q：`.nojekyll` 文件是干嘛的？**  
A：告诉 GitHub Pages 不要用 Jekyll 处理，否则下划线开头的文件（如 `js/` 里的模块）可能无法发布。这个文件已包含在仓库中，不要删除。

---

*最后更新：2025年7月*
